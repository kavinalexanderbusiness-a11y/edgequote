.env.local
.env
.mcp.json
node_modules/
.next/
out/
*.tsbuildinfo
.DS_Store
# generated esbuild bundles (scripts/*.ts → node)
scripts/.bench.cjs
scripts/.repro.cjs

.vercel
.env*

# Supabase CLI local state (linked project ref, service versions) — machine-specific
supabase/.temp/
# Local git worktrees used for parallel branch work
.claude/worktrees/

# Invoice-detail measurement output (scripts/inv-cdp.mjs)
.invbefore/
.invafter/
.chrome-profile/

# Quote-builder measurement output (scripts/qb-cdp.mjs)
.qb-before/
.qb-after/
.inbox-harness/

# CDP investigation output (screenshots / measurements)
search-*.png
search-cdp-*.txt
.chrome-search-*/
.dayactions-harness/

# Session fixture credentials (worker passwords for authenticated E2E proofs).
# ⛔ Never commit: these are real logins on the production project.
scripts/.s*-fixture.json
